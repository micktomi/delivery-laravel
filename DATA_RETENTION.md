# Retention προσωπικών δεδομένων παραγγελιών

Οι παραγγελίες διατηρούν οικονομικό και operational ιστορικό, αλλά περιέχουν PII: όνομα, email, τηλέφωνο, διεύθυνση, όροφο/κουδούνι και ελεύθερες σημειώσεις παραγγελίας ή είδους.

Η εφαρμογή δεν επιλέγει νομική περίοδο retention. Ο υπεύθυνος επεξεργασίας πρέπει να ορίσει την περίοδο με βάση τις υποχρεώσεις του και, όταν είναι έτοιμος, να ρυθμίσει:

```env
ORDER_PII_ANONYMIZATION_DAYS=0
```

Η τιμή `0` (default) απενεργοποιεί την αυτοματοποίηση. Θετική τιμή σε ημέρες ενεργοποιεί το καθημερινό `orders:anonymize-personal-data` για παραγγελίες `completed` ή `cancelled` με `placed_at` παλαιότερο ή ίσο από το όριο.

Η διαδικασία είναι idempotent και αλλάζει μόνο PII: χρησιμοποιεί μη προσωπικά placeholders στα υποχρεωτικά πεδία και αφαιρεί email, όροφο/κουδούνι και free-text notes. Διατηρεί status, είδη/τιμές, σύνολα, εκπτώσεις, timestamps, Viva order/transaction references και payment data. Το `personal_data_anonymized_at` δείχνει ότι η παραγγελία έχει ήδη επεξεργαστεί.

Πρόσβαση: το Filament order history παραμένει για `is_admin` χρήστες. Η κουζίνα χρειάζεται μόνο όνομα και preparation notes για ενεργές παραγγελίες. Οι διαθέσιμες διανομές δεν εμφανίζουν πλέον customer PII στον οδηγό· πλήρη delivery στοιχεία εμφανίζονται μόνο μετά την ανάθεση στον συγκεκριμένο οδηγό.
